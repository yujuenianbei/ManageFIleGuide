'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getMatchAllITC = getMatchAllITC;

var _graphqlCompose = require('graphql-compose');

var _utils = require('../../utils');

function getMatchAllITC() {
  var opts = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

  var name = (0, _utils.getTypeName)('QueryMatchAll', opts);
  var description = (0, _utils.desc)(`
    The most simple query, which matches all documents,
    giving them all a _score of 1.0.
  `);

  return (0, _utils.getOrSetType)(name, function () {
    return _graphqlCompose.InputTypeComposer.create({
      name,
      description,
      fields: {
        boost: {
          type: 'Float'
        }
      }
    });
  });
}